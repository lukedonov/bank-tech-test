describe("Statement", function() {
  var statement;

  beforeEach(function() {
    statement = new Statement()
  })

  // it("statement is an empty array", function() {
  //   expect(statement.transactionHistory).toEqual([])
  // })
})